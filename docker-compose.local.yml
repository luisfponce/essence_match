services:
  backend:
    ports:
      - "${BACKEND_PORT:-8000}:8000"
  frontend:
    ports:
      - "${FRONTEND_PORT:-8080}:80"
  postgres:
    ports:
      - "${DB_PORT:-5432}:5432"
  redis:
    ports:
      - "${REDIS_PORT:-6379}:6379"
